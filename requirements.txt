# ---------------------------------------------------------------------------
# Dependencias de ejecución (runtime)
# ---------------------------------------------------------------------------
# Pines en >= a versiones verificadas que instalan en Python 3.12 - 3.14.
# (Las versiones exactas antiguas no compilaban en Python 3.14: pydantic-core
#  fallaba al construirse desde fuente.)
fastapi>=0.118                 # framework de la API
uvicorn[standard]>=0.34        # servidor ASGI
sqlalchemy>=2.0.36             # ORM
pydantic>=2.10                 # validación de esquemas
starlette>=0.40                # >=0.40 corrige CVE-2024-47874 (DoS multipart sin límite)
python-multipart>=0.0.18       # subida de ficheros (FastAPI); >=0.0.18 corrige CVE-2024-53981

# ---------------------------------------------------------------------------
# Solo para la suite de tests (backend/tests/): no se necesita en producción
# ---------------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                     # cliente usado por el TestClient de FastAPI/Starlette
